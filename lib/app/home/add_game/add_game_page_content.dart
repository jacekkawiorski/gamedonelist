import 'package:flutter/material.dart';

class AddGamePageContent extends StatelessWidget {
  const AddGamePageContent({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dwa'),
    );
  }
}
